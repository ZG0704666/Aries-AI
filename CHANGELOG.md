# Changelog

## 1.3.1 (2026-02-16)

### 新增
- 新增“第三方 OpenAI 兼容 API”配置入口（保持原有主 API 配置不变），可在侧边栏独立配置 Base URL 和 Model。
- 对侧边栏 API 检测/发送链路补充动态参数透传：主界面与浮窗服务共用同一份 API 配置读取与解析逻辑。

### 修复
- 修复历史回归导致的 API 签名误判问题，优化版本校验时的签名比较策略。

### 兼容
- `AutoGlmClient` 继续保留原有接口能力，并新增对可配置网关地址与模型的兼容参数，兼容既有调用方使用方式。
