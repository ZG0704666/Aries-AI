FROM vllm/vllm-openai:v0.12.0

# Zhipu official notes (in Open-AutoGLM): after installing vLLM, upgrade transformers to 5.0.0rc0 (or newer pre-release).
# Dependency conflicts around transformers can be ignored.
RUN pip install -U transformers --pre
